spring.application.name=backend

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# ===========================
# DATABASE (MATCHES XAMPP 3307)
# ===========================
spring.datasource.url=jdbc:mysql://localhost:3307/cinema?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kuala_Lumpur
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===========================
# EMAIL (SAFER DEFAULT FOR NOW)
# ===========================

# Use mock mode while testing (recommended)
cinema.email.mock=true

# Gmail SMTP (only works if you turn mock=false later)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cinemabookingsystem123@gmail.com
spring.mail.password=ksbjvpktgqnpcwwf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
